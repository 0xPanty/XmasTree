# 邮票系统设计文档

## 📮 邮票类型

### 三种基础邮票（每个0.2 USDC）

| 邮票 | 免费条件 | 不符合时价格 |
|------|---------|------------|
| 🟣 **Neynar邮票** | Neynar Score > 0.6 | 0.2 USDC |
| 🟪 **Farcaster邮票** | 持有 Farcaster Power Badge | 0.2 USDC |
| 🎨 **Warplet邮票** | 持有 Warplet NFT | 0.2 USDC |

### 成就邮票

| 邮票 | 解锁条件 | 价格 |
|------|---------|------|
| 🌟 **Based邮票** | 拥有以上三种邮票 | **FREE** |

**Based邮票**是成就邮票，只有收集齐三种基础邮票才能解锁，给用户强烈的成就感！

---

## 🎯 业务流程

```
用户点击"Send to Friends"
  ↓
检查用户持有的邮票
  ↓
┌────────────────────────────────────┐
│ 情况1: 没有任何邮票               │
│ → 显示邮票商店                    │
│ → 检查每个邮票的免费资格          │
│ → 符合条件显示"✨ FREE (Eligible)" │
│ → 不符合显示"💰 0.2 USDC"        │
│ → 必须至少获得1个邮票才能继续     │
└────────────────────────────────────┘
  ↓
┌────────────────────────────────────┐
│ 情况2: 有1-2个邮票                │
│ → 显示邮票选择器                  │
│ → 选择一个已有的邮票              │
│ → 或购买其他邮票扩展收藏          │
└────────────────────────────────────┘
  ↓
┌────────────────────────────────────┐
│ 情况3: 有全部3个基础邮票 🎉       │
│ → 🎊 解锁Based OG邮票！          │
│ → 显示4个邮票选择器               │
│ → "You're a Based OG Collector!" │
└────────────────────────────────────┘
  ↓
选择邮票后 → 生成AI图片
  ↓
Canvas合成：AI图片 + 选择的邮票PNG
  ↓
发送给朋友 ✅
```

---

## 💾 数据结构

### LocalStorage存储
```javascript
// 用户的邮票收藏
{
  fid: 12345,
  stamps: {
    neynar: {
      owned: true,
      acquired_method: 'free', // 'free' or 'paid'
      acquired_at: '2025-12-21T08:00:00Z'
    },
    farcaster: {
      owned: false
    },
    warplet: {
      owned: true,
      acquired_method: 'paid',
      acquired_at: '2025-12-21T08:30:00Z',
      tx_hash: '0x...'
    },
    based: {
      owned: false, // 自动解锁当三个都有
      unlocked: false
    }
  }
}
```

---

## 🎨 UI设计

### 邮票商店Modal（没有邮票时）

```
┌──────────────────────────────────────┐
│  🚫 需要邮票才能生成明信片            │
│  Get your first stamp to continue    │
├──────────────────────────────────────┤
│ 🟣 Neynar Stamp                      │
│ ┌────────────────────────────────┐   │
│ │ [邮票图片预览]                  │   │
│ │ ✨ FREE - You're Eligible!      │   │
│ │ Active user with 150 followers  │   │
│ │ [✅ Claim Neynar Stamp]         │   │
│ └────────────────────────────────┘   │
├──────────────────────────────────────┤
│ 🟪 Farcaster Stamp                   │
│ ┌────────────────────────────────┐   │
│ │ [邮票图片预览]                  │   │
│ │ 🔒 0.2 USDC                     │   │
│ │ Requires Power Badge            │   │
│ │ [💰 Buy for 0.2 USDC]           │   │
│ └────────────────────────────────┘   │
├──────────────────────────────────────┤
│ 🎨 Warplet Stamp                     │
│ ┌────────────────────────────────┐   │
│ │ [邮票图片预览]                  │   │
│ │ 🔒 0.2 USDC                     │   │
│ │ Requires Warplet NFT            │   │
│ │ [💰 Buy for 0.2 USDC]           │   │
│ └────────────────────────────────┘   │
└──────────────────────────────────────┘
```

### 邮票选择器（已有邮票时）

```
┌──────────────────────────────────────┐
│  Choose Your Stamp                   │
│  Select which stamp to use           │
├──────────────────────────────────────┤
│ ◉ 🟣 Neynar Stamp ✅ Owned          │
│ ○ 🟪 Farcaster Stamp 🔒 Get it      │
│ ◉ 🎨 Warplet Stamp ✅ Owned         │
├──────────────────────────────────────┤
│ [Continue →]                         │
└──────────────────────────────────────┘
```

### Based OG解锁通知

```
┌──────────────────────────────────────┐
│            🎊 CONGRATULATIONS! 🎊    │
│                                      │
│   You've collected all 3 stamps!    │
│                                      │
│   🌟 BASED OG STAMP UNLOCKED! 🌟    │
│                                      │
│   [View Your Collection →]          │
└──────────────────────────────────────┘
```

---

## 🔧 技术实现

### 1. 检查邮票所有权
```javascript
function getUserStamps(fid) {
  const key = `stamps_${fid}`;
  const stamps = JSON.parse(localStorage.getItem(key) || '{}');
  
  // 检查是否解锁Based OG
  const hasAll = stamps.neynar?.owned && 
                 stamps.farcaster?.owned && 
                 stamps.warplet?.owned;
  
  if (hasAll && !stamps.based?.owned) {
    stamps.based = {
      owned: true,
      unlocked: true,
      acquired_method: 'achievement',
      acquired_at: new Date().toISOString()
    };
    localStorage.setItem(key, JSON.stringify(stamps));
    showBasedOGUnlockNotification();
  }
  
  return stamps;
}
```

### 2. Canvas合成邮票
```javascript
async function compositeStampOnImage(base64Image, stampType) {
  const stamp = new Image();
  stamp.src = `/stamp/${stampType}.png`; // neynar/farcaster/warplet/based
  // ... 合成逻辑
}
```

### 3. 购买邮票
```javascript
async function buyStamp(stampType) {
  // 调用Farcaster Frame交易API
  const tx = await sendTransaction({
    to: PAYMENT_ADDRESS,
    amount: '0.2',
    token: 'USDC'
  });
  
  if (tx.success) {
    saveStampOwnership(stampType, 'paid', tx.hash);
  }
}
```

---

## 📊 数据分析价值

### 邮票获取统计
- 多少用户获得免费Neynar邮票（高质量用户）
- 多少用户有Power Badge（付费用户）
- 多少用户持有Warplet（NFT收藏家）
- 多少用户解锁Based OG（超级用户）

### 收入模型
- 免费邮票转化率
- 付费邮票购买率
- 平均每用户邮票数量
- 基于邮票的用户分层

---

## 🚀 后续扩展

1. **季节性邮票**：圣诞限定、春节限定
2. **合作邮票**：与其他项目合作定制邮票
3. **邮票交易**：二级市场交易稀有邮票
4. **邮票展示**：个人主页展示邮票收藏
5. **邮票成就系统**：更多收藏成就和徽章
